rules_version = '2';

// The app never talks to Firestore directly — only the syncApi Cloud
// Function does, via the Admin SDK (which ignores these rules entirely).
// Deny all direct client access.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
